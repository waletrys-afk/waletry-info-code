export default function Onsite({ zagl, text }) {
  return (
    <>
      <section className="onsite-css">
        <h1>{zagl}</h1>
        <span>{text}</span>
      </section>
    </>
  );
}
